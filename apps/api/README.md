# @app/api

Fastify REST API. Producers for BullMQ queues. Workers run from `src/worker.ts`.

```
pnpm dev          # http://localhost:3001
pnpm dev:worker   # BullMQ worker, same Redis
```

See [CLAUDE.md](./CLAUDE.md).
