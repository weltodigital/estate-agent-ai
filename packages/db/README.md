# @app/db

Supabase schema, migrations, generated types, and a typed Supabase client factory.

```
migrations/    Numbered SQL migrations (applied via supabase db push)
src/types.ts   Generated TS types (pnpm db:types)
src/client.ts  Server / browser / service client factories
```

See [CLAUDE.md](./CLAUDE.md).
